@Library('robot-shared-library@main') _

env.REPONAME="user"
env.TF_DIRECTORY="mutable-infra"   // This value  only needed by Backend COmponents ; For Infra, you don't need it, becuase the code in the root directory.
terraform()